・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。


●ムービー。再生中のキーパッドタップでフェードアウトさせた際、音はフェードアウトするが、映像はもう一度最初から再生されているように見える。

●ムービー。静止画も再生可能にする。出来ることは、表示とフェードアウトだけ。
読まれたファイルが静止画だった場合、パッド上に音量・時間を表示しない。

●BANK詳細設定の背景色が９色ある。９色目は１色目と同じなので不要。
　→１列目が７色で２列目が１色は配置が不自然。１列にまとめて。（ウィンドウの横幅を広げて）

●動画・画像要素を含むファイルが読み込まれた場合、自動的にカテゴリを「MOV」にする。（その後の変更は可能）


