・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。


・アプリのアイコンを、 tmp/ikepon_icon.png を元に作成して設定して。

・パッド詳細設定。テキストボックスにフォーカスが当たっている時に「OK」ボタンなどが押せない。また、他のテキストのドラッグ数値変更が出来ない。フォーカスが移動せず前のテキストボックスの値が変わってしまう。
　テキストボックス外でのマウスクリックで、即座にフォーカスを解除して。
　パッド詳細設定だけでなく、他のサブ画面も同様に見直して。

・キーパッド再生時の右クリックメニュー。項目を「フェードアウト」「カットアウト」に変更。

・ミキサーのメモリーボタン。右クリックメニュー。項目を「フェード移動」「即座に移動」「再登録」「削除」に変更。現在、フェード移動が無いが追加する。（左クリックでフェード移動は出来るが、右クリックでも出来るようにする）

・パッド詳細設定で追加された「ループ再生開始位置」、上下のスキマが他の項目と会っていない。揃えて。

・パッド詳細設定、再生終了位置で右クリック→「初期値に戻す」を選択すると、総時間になってしまう。空欄にして。


・ミキサー操作子のフォーカスは、パッド内の音量をドラッグやホイールで変更したときにリセットされていない。とにかく、該当フェーダー以外で何らかのアクションがあったら必ずリセットすること。

