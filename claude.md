・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。



●デバッグ中はローカルのどこかにデバッグ用ログを残しておき、あとで参照できるようにできない？本番リリース時のみ機能削除する感じで。

●アプリケーション名にアクセント記号を入れて、「ikéPon」に変更。

●BANK切り替え時に現在は必ず警告が出るが、これを「現在動作中のパッドが無い場合に限り」削除。即座に変更する。
現在動作中のパッドがある場合、従来通り警告が出る。「YES」選択で、動作中のパッドはすべて即座に停止してバンクを切り替える。

●FADE/CUTがカットモードであっても、スタンバイ画像のフェードイン時間は有効にする。
●FADEモードの際に、スタンバイ画像が表示されないケースがあった。確認しておいて。

。


PAセパレートモードがONになると動画が表示されない。
→●OKなときとNGな時があるようだ。もしかして、内部的に切り替えが間に合っていないうちに再生を始めるとおかしくなるとか？

・パッドの詳細設定で、動画の再生開始位置を後ろの方に設定したところ、再生したら動画が真っ黒だった。２回目に再生したときは、動画が表示された。バッファリングが間に合っていないのでは？もしそうであったら、バッファリング中はパッド操作が出来ないようにして。
→●直ってない！

●パッド詳細設定、再生終了位置が空欄の時にマウスドラッグ・ホイール操作が行われ場合、ファイル終端からスタートするようにして。現在は0:00.0になっている。

