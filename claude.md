・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・docsフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・mdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。もちろん、このmdファイルも含む。


・MOVで再生終了位置を設定したとき、終了後もプログレスバーが動き続ける。終わった後もプログレスバーが動き続ける。（映像や音声はちゃんと止まる）
・MOVで、詳細設定で「最終フレームで止める」を設定した際、最終フレームで自動的に一時停止という扱いと考え、黄色点滅状態にして。
・MOVが一時停止（「最終フレームで止める」も含む）状態の時、ALL FADEが実行された場合、音は無音のまま映像だけフェードアウトして。
・MOVが一時停止（「最終フレームで止める」も含む）状態の時、右クリック→「フェードアウト」を実行可能にする。挙動は前項(ALL FADE)と同じで、音は無音のまま映像だけフェードアウト。BGM,SEは今まで通り、一時停止状態でのフェードアウトは不可。ALL FADEはカットアウトと同じ挙動）

・全体設定の「言語変更はアプリ再起動～」「別ファイルに変更：～」「オーディオ設定の変更は～」の色が薄すぎる。「スタンバイ画像」と同じ色にして。パッド詳細設定の「別ファイルに変更：～」も同じ色にして。

・全体設定で言語変更・オーディオ設定変更があって「OK」が押された場合、インフォメーションに「設定を反映させるため、再起動しますか？(YES ? NO)」のような警告を表示。YESだったらそのまま再起動して。なお、言語変更時は、この警告を常に「日本語と英語の両方で表示」して。

・マニュアルを英語に対応して。

・ビルドフォルダを ./dist に変更して。 C:\Users\ike09\.claude\claude.md に書いてあると思う。
