・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。


●mp4、再生はされるようになった。しかし、
フェードアウトやプログレスバーによる再生位置変更に対応できていない。
今、音と絵が別々のルーチンになっているのが原因か？

・音量表示を「0～200」ではなく「0%～500%」にする。デフォルトが100%なのは変わらないが、音量が非常に小さいものにも対応できるようにする。パッド上部、詳細設定ともに有効。

・パッド上部の音量表示「100%」のフォントサイズを、カテゴリ表記と同じにする。
・ついでに、時間表示のフォントサイズもカテゴリ表記と同じにする。



●BANK詳細設定の背景色が９色ある。９色目は１色目と同じなので不要。

