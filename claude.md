・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。


・パッド詳細設定。テキストボックスにフォーカスが当たっている時に、他のテキストのドラッグ数値変更が出来ない。フォーカスが移動せず前のテキストボックスの値が変わってしまう。
　テキストボックス外で何らかのアクションがあったら、即座にフォーカスを解除して。
　パッド詳細設定だけでなく、他のサブ画面も同様に見直して。設定メニューでも同様のことが起きていることを確認済み。

・設定メニュー。「フェードアウト時間」の項目を一番上に移動。また、初期値を3.0秒から2.0秒に変更。
・設定メニュー。「スタンバイ画像フェードイン」の項目名を「スタンバイ画像フェードイン時間」に変更。


・パッド詳細設定で追加された「ループ再生開始位置」、直下の仕切り線との間のスキマが狭すぎる。他と揃えて。

・ミキサー操作子のフォーカス。「該当フェーダー以外で何らかのアクションがあったら必ずリセット」だが、少なくとも以下のケースでリセットされていないことを確認した。
　・何もないところでマウスクリック
　・バンク変更
　・M1ボタン押下
　とにかく、他のすべてのアクションでリセットして。
