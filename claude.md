・C:\Users\ike09\.claude\claude.md 初回起動時と更新あり時は必ず読むこと。
・tmpフォルダに拡張子mdの仕様書がある。初回起動時と更新あり時は必ず読むこと。
・このmdファイルの内容を書き換える時、元のファイルを必ず「.md.bak」で残すこと。「.md.bak」の上書きは許可する。


・ALL CUTボタンで画像表示ウィンドウは閉じないようにする。

・MUボタンはグレーアウトさせず、常に有効色にしておく。文字の色は白、背景色は赤（カットアウト時の背景色と同じ）。

・ミキサー操作子のフォーカスは、該当フェーダー以外の操作（他のパッドが押される、左・右クリック、パッド内の音量変更など）でリセットする。

・パッド詳細設定の「再生終了位置」、初期値は空欄にする。
・パッド詳細設定の「再生終了位置」の下に、「ループ再生開始位置」欄を追加。初期値は空欄。ここに数字が入った場合、２ループ目以降はその時間から再生する。なお、「最後まで再生した後」が「ループ再生」になっていない時はグレーアウト。

