<Window x:Class="ikePon.UI.Dialogs.BankDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="バンク詳細設定" Width="360" SizeToContent="Height"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#212121" Foreground="White"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="16">

        <!-- 表示名 -->
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="表示名" Grid.Column="0"/>
            <TextBox x:Name="TbDisplayName" Grid.Column="1"/>
        </Grid>

        <!-- 背景色 -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="背景色" Grid.Column="0"/>
            <WrapPanel Grid.Column="1" x:Name="BgColorSwatchesPanel"/>
        </Grid>

        <Separator Background="#383838" Margin="0,0,0,12"/>

        <!-- OK / キャンセル -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnOk" Content="OK" Width="80" Margin="0,0,8,0"
                    Click="BtnOk_Click"
                    Background="#1C446E" BorderBrush="#3A9FFF" Foreground="White"
                    Padding="0,4" Cursor="Hand"/>
            <Button x:Name="BtnCancel" Content="キャンセル" Width="80"
                    Click="BtnCancel_Click"
                    Background="#3C3C3C" BorderBrush="#555" Foreground="#CCC"
                    Padding="0,4" Cursor="Hand"/>
        </StackPanel>

    </StackPanel>
</Window>
